# Archived SAM 3.1 client preprocessing

This directory preserves the retired SAM 3.1 cloud/local preprocessing implementation.
It is not imported, packaged, deployed, or exposed by the active modal-3D-client runtime.
The active preprocessing path is local `rembg` global matting followed by canonical RGBA normalization.
